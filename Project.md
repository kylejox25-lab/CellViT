# RxRx3-core 六通道细胞图像的掩码表征学习

> 状态：研究设计 v0.3。训练资源为服务器单张 RTX 3090（24 GB）；服务器已可读取 RxRx3-core，连续训练时间暂不设上限。本地数据目录 `E:\CellPainting\rxrx3_core` 只读；服务器上的数据同样按只读使用。代码、索引、缓存、检查点和结果写在项目目录或服务器另设的可写目录。

实施进度：Data loader 第一阶段已写入 `src/rxrx3_mae/data/`，支持原始 Parquet→孔位级 MDS 转换和 Streaming 读取；已补 [Conda 环境](environment.yml) 和 [数据检查 notebook](notebooks/01_explore_rxrx3_core.ipynb)，使用方法见 [README.md](README.md)。本地已验证原始数据键、图像解码及 notebook，服务器上的真实 MDS 读回尚待执行。模型训练阶段尚未开始。

## 1. 研究目标与问题

核心产物是一个可复用的六通道 Cell Painting 图像**基座编码器**：输入单孔图像，输出裁剪级、孔位级 embedding 和可选的 patch token，供后续检索、聚类、冻结探测及迁移使用。当前只用 RxRx3-core 的图像和自带元数据训练、选择和评估模型；不引入外部标注、其它图像数据或预训练权重。这里的“基座”指可供多种下游任务复用的域内表征，跨细胞类型、跨实验平台的泛化能力需未来用独立数据证明。

核心研究问题是：**在相同编码器、图像、训练步数和计算预算下，跨通道掩码是否比纯空间掩码产生更有用、更稳健的表征？** 先建立纯空间 MAE，再评估跨通道掩码；对照辅助学习作为后续扩展。模型选择依赖数据集内部可计算的跨 guide 基因表型检索、跨板重复样本检索和扰动信号；重建误差仅监控优化过程。

**预期交付物**：可复现的数据索引与读取器、从零训练的模型配置与权重、独立推理接口、按孔位输出的 embedding、固定版本的域内评测脚本、消融结果和模型说明。保留最佳模型、训练记录和至少一个完整训练过程的检查点；本文不预设改进一定有效。

## 2. 数据事实与边界

本地数据已经核对：

| 项目 | 实际情况 | 设计影响 |
| --- | --- | --- |
| 图像 | `data/train-00000-of-00035.parquet` 等 35 个分片，共 1,335,606 个单通道 JP2 文件；每孔六通道，512×512、8 bit | 一个模型样本必须先按孔位将六个通道按固定顺序组装；不能把每个 JP2 当成一张独立的六通道图 |
| 键 | 图像 `__key__` 例：`compound-001/Plate1/AA15_s1_1`；元数据 `well_id` 例：`compound-001_1_AA15` | 根据实验名、板号和地址构造 `well_id`；末尾 `_1` 至 `_6` 是通道号，不能混入孔位 ID |
| 元数据 | 222,601 个唯一 `well_id`；字段包括实验、板、孔位、基因、处理、SMILES、浓度、扰动类型和对照类型 | 分组、对照匹配与评测以孔位元数据为准 |
| 预计算特征 | OpenPhenom 384 维、MAE-L 1024 维、MAE-G 1664 维，均为 222,601 孔；CellProfiler 952 维，覆盖 217,758 孔 | 可直接复用作参考基线；CellProfiler 的比较需使用交集孔位，并同时报告覆盖率 |
| 标注 | 包含 CRISPR 和 COMPOUND 扰动、对照与浓度；无现成 MoA、毒性或单细胞状态标签 | 这些任务不能直接作为本项目的主监督指标 |

论文介绍的核心规模为 222,601 孔、1,674 种化合物、约 736 个基因靶标。元数据中可见 12 个不同的浓度数值；论文所述“8 个浓度”是常规化合物浓度系列，少量非标准浓度不能硬编码排除。对照处理名包含 `EMPTY_control` 和 `CRISPR_control`；不能将其直接等同于 DMSO。所有对照定义必须联合 `perturbation_type`、`well_type_label`、实验和孔板核验。

本地文件不包含官方药物—基因关联评测所需的全部外部真值。因此当前不运行依赖外部关联标注的 EFAAR 基准，也不把它作为模型选择依据。提供的 OpenPhenom、MAE-L、MAE-G 与 CellProfiler 特征只作评测方法的参考和结果背景，不用于模型训练或蒸馏。使用数据或公开结果时核对数据目录中的 `LICENSE`，并在报告中按许可要求注明来源。

## 3. 研究假设与验收标准

1. **H1：跨通道掩码有益。** 在相同参数量、训练步数、输入预处理和评测流程下，空间 + 通道掩码模型提高跨 guide 同基因与跨板同扰动检索指标；比较固定样本上的配对差值与置信区间，不用单次最佳结果下结论。
2. **H2：改善不是批次伪信号。** 改进模型的跨板同扰动一致性提高或保持，且在分别评估原始特征和相同批次校正后的特征时结论稳健。
3. **H3：对照辅助信号可能有益。** 仅在 H1 完成后评估；若对照匹配不足或结果不稳，则不纳入最终模型。

最低验收：从零训练、断点续训和独立推理均可从配置重现；全部有效孔位均有且只有一个 embedding；固定测试集上完成跨 guide、跨板检索及批次分析；保留最优编码器和模型说明；每项改进有配对消融和训练成本记录。单一数据集内成功只支持“RxRx3-core 域内基座”的结论。

## 4. 数据流程

### 4.1 索引与质检

1. 流式扫描 35 个 Parquet 分片，仅保存 `__key__`、分片号、行组/行号与解析后的孔位、通道号索引到项目目录。校验每孔恰有 `_1` 至 `_6` 六个通道，键无重复、都能连接到元数据。正式训练前统计缺失、异常与解码失败；异常孔剔除并记录清单。
2. 按固定通道顺序读取 JP2 字节并解码，组装为 `6×512×512`。先用若干随机孔可视化核对顺序、方向、强度范围和图像质量。图像输入在训练和推理时使用同一预处理。
3. 主方案从每孔 512×512 中取四个不重叠的 256×256 六通道区域。训练时从四块中抽样，并可使用同步于全部通道的旋转/翻转；推理时四块分别编码、取均值得到孔位 embedding。此方案便于和官方“4 块 256×256 后取均值”的特征生成过程比较。若改用全图，须单独报告算力及结果。
4. 强度标准化只用训练范围内预先确定的统计量，先尝试逐通道稳健分位数裁剪与缩放。分位数、裁剪阈值和通道映射都写入配置。避免任意逐图归一化、通道置换或强烈颜色扰动抹掉真实表型差异。

### 4.2 数据隔离与版本

- **开发阶段**：按 `(experiment_name, plate)` 分组，在每个实验内预留验证板和测试板；各板的全部孔位、六通道及四个裁剪只能进入同一个集合。分配比例先以约 80%/10%/10% 为目标，再按真实板数和查询扰动覆盖率调整并冻结清单。训练阶段的模型不读取验证/测试图像，验证集用于选择超参数和停止点，测试集只在方案冻结后使用。
- **检索定义**：测试板孔位作查询，库中使用其它板的孔位或由其形成的处理原型；查询与库绝不共享同一孔位或同一图像裁剪。跨 guide 基因检索还必须排除相同 guide。要明确库中训练板图像曾参与无标签预训练，因此该指标衡量新板查询的泛化，不能宣称所有检索图像均未在训练中出现。
- **最终发布模型**：架构、预处理和训练停止规则锁定后，可用 RxRx3-core 全部图像从零再训练以充分利用数据。开发阶段的测试指标对应开发模型；最终全量模型的域内指标属于转导式检查，不能伪称独立测试性能。
- 如做冻结线性探测，探测器的训练/验证/测试按板分开，必要时再按 guide 或处理身份分开；编码器始终冻结。冻结索引、分片清单、预处理、随机种子、模型和评测版本。

## 5. 模型与训练

### 5.1 共同编码器和纯空间 MAE 基线

单张 3090 的首选配置是**从零初始化的 ViT Small/16 编码器 + 轻量 MAE 解码器**。每个 256×256×6 区域被切为 16×16 的六通道 patch，共 256 个 token；随机隐藏约 75% token，编码器只处理可见 token，解码器重建隐藏区域。损失是掩码 patch 上逐通道归一化的像素重建误差，避免亮度较强的通道主导。先保持输入分辨率、patch 大小和模型宽度固定，优先把数据读取、训练稳定性和表征评测做扎实；是否升级到更大的编码器由显存及收益决定。

建议起点：FP16 自动混合精度与梯度缩放、AdamW、余弦学习率衰减和约 5% 预热；每卡 micro-batch 从 8 起试，逐步试到 16，使用梯度累积达到约 128 的有效 batch。优化器学习率先从 `1.5e-4` 做短程试验，具体值按有效 batch 和训练曲线调整。出现 OOM 时先降低 micro-batch，再启用激活检查点；记录实际峰值显存、图像/秒、数据等待占比。所有正式消融使用同一优化设置和处理过的图像数。

以“一个 epoch 中每个训练孔随机取一个 256×256 裁剪”为采样单位，跨 epoch 轮换四个裁剪。先跑 1 个 epoch 的端到端试验验证无数值异常和解码瓶颈，再跑约 10 个 epoch 的开发试验；正式训练以验证集表征指标进入平台期为停止依据，按约 50–100 epoch 预留训练范围并根据实测速率调整。每个固定周期保存可续训检查点、记录重建损失和验证表征指标；训练时间无硬上限，但不能仅靠继续降低像素损失无限延长。

推理时输入完整六通道图像，取编码器的 patch token 平均池化向量；解码器丢弃。若比较 `[CLS]` 与平均池化，须在开发集预先确定一种，不按最终测试结果选择。

### 5.2 跨通道掩码：核心改进

在相同编码器上增加通道缺失训练。每个训练样本以预先固定概率选择“纯空间掩码”或“隐藏 1 个完整通道 + 剩余可见通道上的空间掩码”。被隐藏通道的位置、通道身份与可见性要显式传给模型；损失分别统计空间隐藏区域与整通道重建区域，再按权重合成，避免整通道损失因像素数多而主导训练。训练时保留一部分纯空间样本，以适配最终完整六通道推理。

纯空间基线与该方案须保持参数量、输入裁剪、总训练图像数和优化步数一致。至少消融：空间掩码基线；空间 + 整通道掩码；以及不同整通道掩码概率。跨通道重建误差可能改善而生物学指标不改善，需如实报告。

### 5.3 对照辅助学习：条件性扩展

先检查每个实验/孔板上相容的阴性对照数量与类型。若覆盖充分，可将同板同条件对照的**群体特征**作为辅助参照，用于表示层残差或对比目标；不把一张未配对的对照细胞图像当作药物图像的逐像素重建目标。对照输入仅用于训练辅助损失时，最终编码器仍独立接收单孔图像，推理不依赖恰好匹配的对照图。此扩展必须与无对照版本做配对消融，并说明对照采样规则。

## 6. 评测方案

### 6.1 主指标：仅用数据集自带元数据

**主指标 A：跨实验、跨 guide 的基因表型检索。** `Query guides` 中有 734 个基因，其中 695 个出现在两个实验、24 个出现在四个实验，具备跨实验检验的基础。测试板上某实验的一个 guide 作查询；候选基因原型仅由**另一个实验**中的孔位组成，且排除相同 guide，所有候选基因来自同一个候选实验。用余弦相似度排序，报告按基因平均的 MRR、Recall@1/5、合格基因数和候选集规模匹配的随机基线。同实验、跨板跨 guide 检索作为较容易的补充指标。先检查各基因的 guide/实验/板覆盖并固定合格规则，防止实验身份或孔板替代基因信号。

**主指标 B：跨板的化合物表型检索。** 测试板的化合物孔作查询，在**相同实验、相同浓度、其它板**的候选孔中检索相同化合物的重复；报告 mAP、Recall@K、合格化合物—浓度对数量及按候选频率匹配的随机基线。重复不足时仅对合格对计算，并报告覆盖率。药物无明显表型时检索失败并不自动说明编码器差；可另在验证集预定的“有扰动信号”子集报告结果，不能用测试结果挑选子集。

两个主指标均在**原始孔位 embedding** 上先算一次，再在相同的对照中心化流程下算一次；主要模型选择看验证集的跨 guide MRR，跨板化合物 mAP 作为约束和补充。比较模型时锁定查询、候选集合、匹配关系及预处理，并通过按基因/化合物分组的 bootstrap 估计不确定性。提供的公开 embedding 可放入同一流程作背景参考，但其训练数据和模型规模不同，不作为从零训练模型的公平对照。

### 6.2 辅助指标

| 任务 | 操作与指标 | 注意事项 |
| --- | --- | --- |
| 扰动信号 | 同扰动重复样本与实验/板匹配的阴性对照之间计算 energy distance，按扰动汇总 | 效应越大不一定代表更有用的表征；与检索指标并列报告 |
| 冻结线性探测 | 冻结编码器，仅用元数据中的基因身份训练线性分类器，报告宏平均准确率/平衡准确率 | 训练和测试按板及 guide 隔离；先核查各基因跨实验覆盖，避免实验批次替代基因信号 |
| 批次稳健性 | 比较跨板同扰动与不同扰动相似度，以及仅在同类型对照孔上的板号可预测性；UMAP 仅辅助展示 | 原始与对照校正特征分开报告；不能用单张二维图判断批次是否消除 |
| 剂量响应 | 对有足够重复的化合物，计算相对匹配对照的表型效应与对数浓度的相关性 | 不预设所有化合物单调；报告适用样本数和非单调案例 |

### 6.3 暂不作为主任务的方向

- **药物—基因活性官方基准、MoA 分类和毒性任务**：依赖当前本地文件没有的关联或类别真值，当前阶段不做。未来若放宽“只用本数据集”的约束，再固定外部标注版本、映射和独立评测规则。
- **SMILES 物化性质回归**：虽能从本数据集 SMILES 生成目标值，但它主要检验能否从表型间接预测化学结构属性，当前不作为基座质量核心指标。
- **单细胞密集预测**：当前提供的是每孔中心裁剪图像，没有单细胞分割或状态真值；需要额外分割与标注数据才能进行严谨评测。
- **对比学习/图像—SMILES 联合模型**：可作后续独立研究方向；不要与纯图像 MAE 的主结论混在一起。

## 7. 实施顺序与产出门槛

| 阶段 | 工作 | 完成门槛 |
| --- | --- | --- |
| 0. 数据审计 | 构造六通道索引、检查连接和解码、核对对照分布、记录许可 | 有可复查的孔位级统计、异常清单及图像样本 |
| 1. 评测先行 | 用本地预计算 embedding 和元数据实现跨 guide、跨板检索及扰动信号指标 | 固定查询/候选/划分清单；随机基线和公开特征结果合理，覆盖率明确 |
| 2. 最小训练闭环 | 用子集训练空间 MAE，完成断点恢复、孔位推理和评测 | 能在同一配置上复跑训练和 embedding；无孔位缺失/重复 |
| 3. 完整基线 | 单张 3090 从零训练空间 MAE，按验证指标选择停止点 | 有编码器权重、推理接口、训练曲线、显存/吞吐与测试评测表 |
| 4. 核心消融 | 只改变通道掩码方案，在相同图像数与优化步数下配对复训 | 给出两项主指标、辅助指标与置信区间，判断 H1/H2 |
| 5. 最终模型 | 锁定配置后用全体图像重新训练，导出权重和孔位特征 | 有模型说明、预处理规范、端到端推理示例和全量覆盖清单 |
| 6. 扩展 | 对照辅助信号或其它内部任务 | 单独报告增益与额外成本，不能改变已冻结的主结论 |

建议代码结构：`src/data/`（索引、解码、预处理）、`src/models/`（MAE 和掩码策略）、`src/train/`、`src/eval/`、`configs/`、`outputs/`。服务器训练可读取已有数据路径，但所有索引、缓存与结果写入可写项目目录。先做数据审计和基准，再开始耗时训练。

## 8. 已确定条件与待核实事项

- 已确定：单张 RTX 3090、24 GB 显存；数据已在服务器可读取；训练时间可按实际效果安排；当前只用 RxRx3-core；目标是可复用的图像基座编码器。
- 实施前核实：服务器实际数据路径、可写输出路径和剩余磁盘空间；服务器操作系统、CUDA/驱动与 PyTorch 环境；数据文件版本与本地是否一致。这些决定读取器和依赖安装方式，不改变研究目标。
- 后续再确定：是否将外部数据集作为独立迁移验证、是否引入外部药物—基因标注做官方基准。这两项均不进入当前训练闭环。

## 9. 依据

- 本地 `E:\CellPainting\rxrx3_core\README.md`、`metadata_rxrx3_core.csv`、图像与 embedding Parquet 的实际结构（2026-09-20 核查）。
- [RxRx3-core 论文](https://arxiv.org/html/2503.20158)：数据构成、预处理和公开基准。
- [官方 EFAAR RxRx3-core 基准](https://github.com/recursionpharma/EFAAR_benchmarking/tree/trunk/RxRx3-core_benchmarks)：药物—基因活性与基因—基因关系评测说明。
- [RxRx3-core 数据集页](https://huggingface.co/datasets/recursionpharma/rxrx3-core)：数据说明与公开特征文件。
